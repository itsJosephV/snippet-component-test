export interface IconTypes {
  width: string;
  height: string;
  fill: string;
}

export interface TWSnippets  {
  title: string,
  githubLink: string,
  twPlaygroudLink: string,
  source: React.ReactElement,
  animation?: string 
}